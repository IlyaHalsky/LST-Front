# Example environment variables for the application

# API base URL for production
# This will be used when the app is built for production
# Leave empty to use relative URLs (when frontend and backend are served from the same domain)
# VITE_API_BASE_URL=https://api.example.com

# For local development, the proxy in vite.config.ts will be used
# No need to set VITE_API_BASE_URL for local development